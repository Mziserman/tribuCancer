# Learn more about services, parameters and containers at
# http://symfony.com/doc/current/book/service_container.html
parameters:

services:
    app.service:
        class: AppBundle\Utils\Service
    app.event:
        class: AppBundle\Utils\Event
    app.association:
        class: AppBundle\Utils\Association
    app.slug:
        class: AppBundle\Utils\Slug