# Learn more about services, parameters and containers at
# http://symfony.com/doc/current/book/service_container.html
parameters:

services:
    app.form.type.color:
        class: AppBundle\Form\Type\ColorChoiceType
        tags:
           - { name: form.type, alias: color_choice }